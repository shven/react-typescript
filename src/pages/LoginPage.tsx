import React from 'react';
import Login from '../components/Login/Login';

export default () => {
    return (
        <section>
            <h2>Login</h2>
            <Login />
        </section>
    );
};
